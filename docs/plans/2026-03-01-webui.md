# Web UI Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add `cw web` command that launches a FastAPI + HTMX local web server for browser-based CRUD management of claude-workspace workspaces.

**Architecture:** FastAPI reads/writes existing `~/.claude-workspace/registry.json` and `.workspace.json` files directly (no new database). HTMX handles partial page updates; Jinja2 renders server-side HTML. Tailwind CSS + HTMX loaded via CDN (no build step).

**Tech Stack:** Python 3 + FastAPI + uvicorn + Jinja2 + HTMX + Tailwind CSS CDN

---

## Prerequisites

- Python 3 installed (macOS ships with Python 3)
- `pip3` available
- All existing `cw` commands still work unchanged after implementation

---

## Task 1: Create web directory and requirements

**Files:**
- Create: `web/requirements.txt`
- Create: `web/__init__.py` (empty)

**Step 1: Create web/ directory with requirements**

```bash
mkdir -p web/templates
touch web/__init__.py
```

Create `web/requirements.txt`:
```
fastapi==0.115.0
uvicorn[standard]==0.30.6
jinja2==3.1.4
python-multipart==0.0.9
```

**Step 2: Verify pip install works**

```bash
cd /path/to/claude-workspace
pip3 install -r web/requirements.txt
```

Expected: All packages install without error. `fastapi`, `uvicorn`, `jinja2` are importable.

**Step 3: Commit**

```bash
git add web/requirements.txt web/__init__.py
git commit -m "feat(web): add web directory and Python requirements"
```

---

## Task 2: Create FastAPI app skeleton with health endpoint

**Files:**
- Create: `web/main.py`

**Step 1: Write `web/main.py`**

```python
#!/usr/bin/env python3
"""claude-workspace Web UI — FastAPI application."""

import json
import os
import subprocess
from pathlib import Path
from typing import Optional

from fastapi import FastAPI, Form, HTTPException, Request
from fastapi.responses import HTMLResponse, RedirectResponse
from fastapi.templating import Jinja2Templates

app = FastAPI(title="claude-workspace Web UI")

CW_DIR = Path(os.environ.get("CW_HOME", Path.home() / ".claude-workspace"))
REGISTRY_FILE = CW_DIR / "registry.json"
WORKSPACE_FILE = ".workspace.json"

templates = Jinja2Templates(directory=str(Path(__file__).parent / "templates"))


# ─── Data helpers ─────────────────────────────────────────────────────────────

def _read_registry() -> list[dict]:
    """Return workspace list sorted by last_used descending."""
    if not REGISTRY_FILE.exists():
        return []
    with REGISTRY_FILE.open() as f:
        data = json.load(f)
    workspaces = data.get("workspaces", [])
    return sorted(workspaces, key=lambda w: w.get("last_used", ""), reverse=True)


def _write_registry(workspaces: list[dict]) -> None:
    """Overwrite registry.json with updated workspace list."""
    tmp = REGISTRY_FILE.with_suffix(".json.tmp")
    with tmp.open("w") as f:
        json.dump({"workspaces": workspaces}, f, indent=2)
    tmp.replace(REGISTRY_FILE)


def _get_workspace(name: str) -> tuple[dict, Path]:
    """Return (registry_entry, workspace_path) or raise 404."""
    workspaces = _read_registry()
    entry = next((w for w in workspaces if w["name"] == name), None)
    if not entry:
        raise HTTPException(status_code=404, detail=f"Workspace not found: {name}")
    ws_path = Path(entry["path"])
    return entry, ws_path


def _read_workspace_json(ws_path: Path) -> dict:
    """Read .workspace.json from workspace directory."""
    ws_file = ws_path / WORKSPACE_FILE
    if not ws_file.exists():
        return {}
    with ws_file.open() as f:
        return json.load(f)


def _write_workspace_json(ws_path: Path, data: dict) -> None:
    """Write .workspace.json atomically."""
    ws_file = ws_path / WORKSPACE_FILE
    tmp = ws_file.with_suffix(".json.tmp")
    with tmp.open("w") as f:
        json.dump(data, f, indent=2)
    tmp.replace(ws_file)


# ─── Health check ─────────────────────────────────────────────────────────────

@app.get("/health")
async def health():
    return {"status": "ok", "registry": str(REGISTRY_FILE)}
```

**Step 2: Verify the skeleton runs**

```bash
cd /path/to/claude-workspace
uvicorn web.main:app --port 8765 --reload
```

In another terminal:
```bash
curl http://localhost:8765/health
```

Expected response:
```json
{"status": "ok", "registry": "/Users/<you>/.claude-workspace/registry.json"}
```

**Step 3: Commit**

```bash
git add web/main.py
git commit -m "feat(web): add FastAPI app skeleton with health endpoint"
```

---

## Task 3: Create base template and dashboard

**Files:**
- Create: `web/templates/base.html`
- Create: `web/templates/index.html`
- Modify: `web/main.py` (add GET `/`)

**Step 1: Create `web/templates/base.html`**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% block title %}claude-workspace{% endblock %}</title>
  <script src="https://unpkg.com/htmx.org@2.0.3"></script>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-950 text-gray-100 min-h-screen font-mono">

  <!-- Nav -->
  <nav class="border-b border-gray-800 px-6 py-3 flex items-center justify-between">
    <a href="/" class="text-orange-400 font-bold text-lg hover:text-orange-300">
      claude-workspace
    </a>
    <a href="/new"
       class="bg-orange-500 hover:bg-orange-400 text-white text-sm px-4 py-1.5 rounded transition-colors">
      + New Workspace
    </a>
  </nav>

  <!-- Content -->
  <main class="max-w-5xl mx-auto px-6 py-8">
    {% block content %}{% endblock %}
  </main>

</body>
</html>
```

**Step 2: Create `web/templates/index.html`**

```html
{% extends "base.html" %}

{% block title %}Workspaces — claude-workspace{% endblock %}

{% block content %}
<div class="mb-6">
  <h1 class="text-2xl font-bold text-white">Workspaces</h1>
  <p class="text-gray-400 text-sm mt-1">{{ workspaces | length }} workspace(s) registered</p>
</div>

{% if workspaces %}
<div class="grid gap-4">
  {% for ws in workspaces %}
  <a href="/workspace/{{ ws.name | urlencode }}"
     class="block bg-gray-900 border border-gray-800 hover:border-orange-500 rounded-lg p-5 transition-colors group">
    <div class="flex items-start justify-between">
      <div>
        <h2 class="text-lg font-semibold text-white group-hover:text-orange-400 transition-colors">
          {{ ws.name }}
        </h2>
        <p class="text-gray-500 text-sm mt-0.5">{{ ws.path }}</p>
      </div>
      <div class="text-right text-xs text-gray-500 shrink-0 ml-4">
        <div>Last used</div>
        <div class="text-gray-400">{{ ws.last_used[:10] if ws.last_used else "—" }}</div>
      </div>
    </div>
    {% if ws.dir_count %}
    <p class="text-gray-500 text-xs mt-3">{{ ws.dir_count }} director(y/ies)</p>
    {% endif %}
  </a>
  {% endfor %}
</div>
{% else %}
<div class="text-center py-20 text-gray-600">
  <p class="text-xl mb-4">No workspaces yet</p>
  <a href="/new" class="text-orange-400 hover:text-orange-300 underline">Create your first workspace</a>
</div>
{% endif %}
{% endblock %}
```

**Step 3: Add dashboard endpoint to `web/main.py`**

Add after the health check endpoint:

```python
@app.get("/", response_class=HTMLResponse)
async def dashboard(request: Request):
    workspaces = _read_registry()
    # Enrich with dir count from .workspace.json
    for ws in workspaces:
        ws_data = _read_workspace_json(Path(ws["path"]))
        ws["dir_count"] = len(ws_data.get("dirs", []))
    return templates.TemplateResponse("index.html", {
        "request": request,
        "workspaces": workspaces,
    })
```

Also add Jinja2 `urlencode` filter after `templates = Jinja2Templates(...)`:

```python
from urllib.parse import quote
templates.env.filters["urlencode"] = lambda s: quote(str(s), safe="")
```

**Step 4: Verify dashboard**

Start server: `uvicorn web.main:app --port 8765 --reload`
Open browser: `http://localhost:8765/`

Expected: Dark-themed page showing registered workspaces as cards (or empty state if none).

**Step 5: Commit**

```bash
git add web/templates/base.html web/templates/index.html web/main.py
git commit -m "feat(web): add dashboard page with workspace card list"
```

---

## Task 4: Workspace detail page

**Files:**
- Create: `web/templates/workspace.html`
- Modify: `web/main.py` (add GET `/workspace/{name}`)

**Step 1: Create `web/templates/workspace.html`**

```html
{% extends "base.html" %}

{% block title %}{{ ws.name }} — claude-workspace{% endblock %}

{% block content %}
<div class="mb-6 flex items-center gap-3">
  <a href="/" class="text-gray-500 hover:text-gray-300 text-sm">← Workspaces</a>
</div>

<div class="bg-gray-900 border border-gray-800 rounded-lg p-6 mb-6">
  <div class="flex items-start justify-between mb-4">
    <div>
      <h1 class="text-2xl font-bold text-white">{{ ws.name }}</h1>
      <p class="text-gray-500 text-sm mt-1">{{ ws.path }}</p>
      {% if ws_data.description %}
      <p class="text-gray-400 text-sm mt-2">{{ ws_data.description }}</p>
      {% endif %}
    </div>
    <div class="text-xs text-gray-500 text-right shrink-0 ml-4">
      <div>Created: {{ ws.created_at[:10] if ws.created_at else "—" }}</div>
      <div>Last used: {{ ws.last_used[:10] if ws.last_used else "—" }}</div>
    </div>
  </div>

  <!-- Action buttons -->
  <div class="flex flex-wrap gap-2 pt-4 border-t border-gray-800">
    <form method="post" action="/workspace/{{ ws.name | urlencode }}/launch">
      <button type="submit"
              class="bg-orange-500 hover:bg-orange-400 text-white text-sm px-4 py-1.5 rounded transition-colors">
        Launch in Terminal
      </button>
    </form>
    <form method="post" action="/workspace/{{ ws.name | urlencode }}/forget"
          onsubmit="return confirm('Remove from registry? (files kept)')">
      <button type="submit"
              class="border border-gray-700 hover:border-gray-500 text-gray-400 hover:text-gray-200 text-sm px-4 py-1.5 rounded transition-colors">
        Forget
      </button>
    </form>
    <form method="post" action="/workspace/{{ ws.name | urlencode }}/delete"
          onsubmit="return confirm('Delete workspace directory? This cannot be undone.')">
      <button type="submit"
              class="border border-red-900 hover:border-red-600 text-red-500 hover:text-red-400 text-sm px-4 py-1.5 rounded transition-colors">
        Delete
      </button>
    </form>
  </div>
</div>

<!-- Directories -->
<div class="bg-gray-900 border border-gray-800 rounded-lg p-6">
  <h2 class="text-base font-semibold text-white mb-4">Directories</h2>

  <div id="dir-list" class="space-y-2 mb-4">
    {% for dir in ws_data.get('dirs', []) %}
    <div class="flex items-center justify-between bg-gray-800 rounded px-3 py-2 text-sm">
      <div>
        <span class="text-gray-200">{{ dir.path }}</span>
        {% if dir.role %}
        <span class="text-gray-500 ml-2">({{ dir.role }})</span>
        {% endif %}
      </div>
      {% if not dir | is_existing_dir %}
      <span class="text-red-400 text-xs ml-2">missing</span>
      {% endif %}
    </div>
    {% else %}
    <p class="text-gray-500 text-sm">No directories added yet.</p>
    {% endfor %}
  </div>

  <!-- Add dir form (HTMX) -->
  <form hx-post="/workspace/{{ ws.name | urlencode }}/add-dir"
        hx-target="#dir-list"
        hx-swap="innerHTML"
        class="flex gap-2">
    <input type="text" name="path"
           placeholder="/path/to/repo"
           class="flex-1 bg-gray-800 border border-gray-700 text-gray-200 text-sm rounded px-3 py-1.5 focus:outline-none focus:border-orange-500">
    <input type="text" name="role"
           placeholder="Role (optional)"
           class="w-36 bg-gray-800 border border-gray-700 text-gray-200 text-sm rounded px-3 py-1.5 focus:outline-none focus:border-orange-500">
    <button type="submit"
            class="bg-gray-700 hover:bg-gray-600 text-gray-200 text-sm px-3 py-1.5 rounded transition-colors">
      Add
    </button>
  </form>
</div>
{% endblock %}
```

**Step 2: Add detail endpoint to `web/main.py`**

Add this Jinja2 filter and the endpoint:

```python
# Add after urlencode filter
templates.env.tests["is_existing_dir"] = lambda d: Path(d["path"]).is_dir()


@app.get("/workspace/{name}", response_class=HTMLResponse)
async def workspace_detail(request: Request, name: str):
    ws_entry, ws_path = _get_workspace(name)
    ws_data = _read_workspace_json(ws_path)
    return templates.TemplateResponse("workspace.html", {
        "request": request,
        "ws": ws_entry,
        "ws_data": ws_data,
    })
```

**Step 3: Verify**

Start server and open `http://localhost:8765/` → click on a workspace card.

Expected: Workspace detail page with directory list and action buttons. If no workspaces exist, create one with `cw setup` first, then refresh.

**Step 4: Commit**

```bash
git add web/templates/workspace.html web/main.py
git commit -m "feat(web): add workspace detail page with directory list"
```

---

## Task 5: New workspace form and create endpoint

**Files:**
- Create: `web/templates/new.html`
- Modify: `web/main.py` (add GET `/new` + POST `/workspace`)

**Step 1: Create `web/templates/new.html`**

```html
{% extends "base.html" %}

{% block title %}New Workspace — claude-workspace{% endblock %}

{% block content %}
<div class="mb-6 flex items-center gap-3">
  <a href="/" class="text-gray-500 hover:text-gray-300 text-sm">← Workspaces</a>
</div>

<div class="bg-gray-900 border border-gray-800 rounded-lg p-6 max-w-xl">
  <h1 class="text-xl font-bold text-white mb-6">New Workspace</h1>

  <form method="post" action="/workspace" class="space-y-4">
    <div>
      <label class="block text-sm text-gray-400 mb-1">Name <span class="text-red-400">*</span></label>
      <input type="text" name="name" required autofocus
             placeholder="My Project"
             class="w-full bg-gray-800 border border-gray-700 text-gray-200 rounded px-3 py-2 text-sm focus:outline-none focus:border-orange-500">
    </div>

    <div>
      <label class="block text-sm text-gray-400 mb-1">Description</label>
      <input type="text" name="description"
             placeholder="Brief description of this workspace"
             class="w-full bg-gray-800 border border-gray-700 text-gray-200 rounded px-3 py-2 text-sm focus:outline-none focus:border-orange-500">
    </div>

    <div>
      <label class="block text-sm text-gray-400 mb-1">Workspace directory <span class="text-red-400">*</span></label>
      <input type="text" name="workspace_path" required
             placeholder="/Users/you/WorkingProjects/my-project"
             class="w-full bg-gray-800 border border-gray-700 text-gray-200 rounded px-3 py-2 text-sm focus:outline-none focus:border-orange-500">
      <p class="text-gray-600 text-xs mt-1">Directory will be created if it doesn't exist.</p>
    </div>

    <div>
      <label class="block text-sm text-gray-400 mb-1">Initial directory (optional)</label>
      <input type="text" name="initial_dir"
             placeholder="/path/to/repo"
             class="w-full bg-gray-800 border border-gray-700 text-gray-200 rounded px-3 py-2 text-sm focus:outline-none focus:border-orange-500">
    </div>

    {% if error %}
    <div class="text-red-400 text-sm bg-red-950 border border-red-900 rounded px-3 py-2">
      {{ error }}
    </div>
    {% endif %}

    <div class="flex gap-3 pt-2">
      <button type="submit"
              class="bg-orange-500 hover:bg-orange-400 text-white text-sm px-5 py-2 rounded transition-colors">
        Create Workspace
      </button>
      <a href="/" class="border border-gray-700 hover:border-gray-500 text-gray-400 hover:text-gray-200 text-sm px-5 py-2 rounded transition-colors">
        Cancel
      </a>
    </div>
  </form>
</div>
{% endblock %}
```

**Step 2: Add new workspace endpoints to `web/main.py`**

```python
import shutil
from datetime import datetime, timezone


@app.get("/new", response_class=HTMLResponse)
async def new_workspace_form(request: Request):
    return templates.TemplateResponse("new.html", {"request": request, "error": None})


@app.post("/workspace", response_class=HTMLResponse)
async def create_workspace(
    request: Request,
    name: str = Form(...),
    description: str = Form(""),
    workspace_path: str = Form(...),
    initial_dir: str = Form(""),
):
    ws_path = Path(workspace_path).expanduser().resolve()

    # Validate name uniqueness
    workspaces = _read_registry()
    if any(w["name"] == name for w in workspaces):
        return templates.TemplateResponse("new.html", {
            "request": request,
            "error": f"Workspace '{name}' already exists.",
        }, status_code=400)

    # Create workspace directory
    ws_path.mkdir(parents=True, exist_ok=True)

    # Build dirs list
    dirs = []
    if initial_dir.strip():
        dirs.append({"path": str(Path(initial_dir.strip()).expanduser().resolve()), "role": ""})

    # Write .workspace.json
    now = datetime.now(timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ")
    ws_data = {
        "name": name,
        "description": description,
        "workspace_path": str(ws_path),
        "created_at": now,
        "dirs": dirs,
    }
    _write_workspace_json(ws_path, ws_data)

    # Write CLAUDE.md stub
    claude_md = ws_path / "CLAUDE.md"
    if not claude_md.exists():
        claude_md.write_text(f"# {name}\n\n{description}\n")

    # Register in registry
    workspaces.append({
        "name": name,
        "path": str(ws_path),
        "created_at": now,
        "last_used": now,
    })
    _write_registry(workspaces)

    return RedirectResponse(url=f"/workspace/{name}", status_code=303)
```

**Step 3: Verify**

Open `http://localhost:8765/new` → Fill in Name + Workspace directory → Submit.

Expected: Redirected to workspace detail page. Check `~/.claude-workspace/registry.json` to confirm entry was added.

**Step 4: Commit**

```bash
git add web/templates/new.html web/main.py
git commit -m "feat(web): add new workspace form and create endpoint"
```

---

## Task 6: Add directory endpoint (HTMX partial update)

**Files:**
- Modify: `web/main.py` (add POST `/workspace/{name}/add-dir`)
- Create: `web/templates/partials/dir_list.html`

**Step 1: Create `web/templates/partials/dir_list.html`**

```bash
mkdir -p web/templates/partials
```

```html
{% for dir in dirs %}
<div class="flex items-center justify-between bg-gray-800 rounded px-3 py-2 text-sm">
  <div>
    <span class="text-gray-200">{{ dir.path }}</span>
    {% if dir.role %}
    <span class="text-gray-500 ml-2">({{ dir.role }})</span>
    {% endif %}
  </div>
  {% if not dir | is_existing_dir %}
  <span class="text-red-400 text-xs ml-2">missing</span>
  {% endif %}
</div>
{% else %}
<p class="text-gray-500 text-sm">No directories added yet.</p>
{% endfor %}
```

**Step 2: Add add-dir endpoint to `web/main.py`**

```python
@app.post("/workspace/{name}/add-dir", response_class=HTMLResponse)
async def add_directory(
    request: Request,
    name: str,
    path: str = Form(...),
    role: str = Form(""),
):
    ws_entry, ws_path = _get_workspace(name)
    ws_data = _read_workspace_json(ws_path)

    resolved = str(Path(path.strip()).expanduser().resolve())
    dirs = ws_data.get("dirs", [])

    # Skip duplicate
    if not any(d["path"] == resolved for d in dirs):
        dirs.append({"path": resolved, "role": role.strip()})
        ws_data["dirs"] = dirs
        _write_workspace_json(ws_path, ws_data)

    return templates.TemplateResponse("partials/dir_list.html", {
        "request": request,
        "dirs": dirs,
    })
```

**Step 3: Verify HTMX partial update**

Open workspace detail → type a directory path in the Add form → click Add.

Expected: Directory list updates in place without full page reload (HTMX swaps `#dir-list` innerHTML).

**Step 4: Commit**

```bash
git add web/templates/partials/dir_list.html web/main.py
git commit -m "feat(web): add HTMX partial update for directory list"
```

---

## Task 7: Launch, forget, delete endpoints

**Files:**
- Modify: `web/main.py` (add three POST endpoints)

**Step 1: Add launch endpoint**

```python
@app.post("/workspace/{name}/launch")
async def launch_workspace(name: str):
    ws_entry, ws_path = _get_workspace(name)

    # Build cw launch command in new Terminal window via osascript
    cw_cmd = f"cw launch '{name}'"
    script = f'tell application "Terminal" to do script "{cw_cmd}"'
    subprocess.Popen(["osascript", "-e", script])

    # Touch last_used in registry
    workspaces = _read_registry()
    from datetime import datetime, timezone
    now = datetime.now(timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ")
    for ws in workspaces:
        if ws["name"] == name:
            ws["last_used"] = now
    _write_registry(workspaces)

    return RedirectResponse(url=f"/workspace/{name}", status_code=303)
```

**Step 2: Add forget endpoint**

```python
@app.post("/workspace/{name}/forget")
async def forget_workspace(name: str):
    workspaces = _read_registry()
    updated = [w for w in workspaces if w["name"] != name]
    if len(updated) == len(workspaces):
        raise HTTPException(status_code=404, detail=f"Workspace not found: {name}")
    _write_registry(updated)
    return RedirectResponse(url="/", status_code=303)
```

**Step 3: Add delete endpoint**

```python
@app.post("/workspace/{name}/delete")
async def delete_workspace(name: str):
    ws_entry, ws_path = _get_workspace(name)

    # Remove from registry
    workspaces = _read_registry()
    updated = [w for w in workspaces if w["name"] != name]
    _write_registry(updated)

    # Delete directory
    if ws_path.exists():
        shutil.rmtree(str(ws_path))

    return RedirectResponse(url="/", status_code=303)
```

**Step 4: Verify each action**

- **Launch**: Click "Launch in Terminal" → new Terminal window opens running `cw launch '<name>'`
- **Forget**: Click "Forget" → confirm → redirected to dashboard; workspace removed from list; directory still exists
- **Delete**: Click "Delete" → confirm → redirected to dashboard; workspace removed from list; directory deleted

**Step 5: Commit**

```bash
git add web/main.py
git commit -m "feat(web): add launch, forget, delete endpoints"
```

---

## Task 8: Create lib/web.sh

**Files:**
- Create: `lib/web.sh`

**Step 1: Write `lib/web.sh`**

```bash
#!/usr/bin/env bash
# web.sh - cw web command: launch local Web UI server

cmd_web() {
  local port="${1:-8765}"
  local cw_web_dir="${CW_WEB_DIR:-$(cd "$(dirname "${BASH_SOURCE[0]}")/../web" && pwd)}"

  # Check Python 3
  if ! command -v python3 &>/dev/null; then
    error "$(t "web_python_required")"
    exit 1
  fi

  # Install requirements if needed
  local req_file="$cw_web_dir/requirements.txt"
  if [[ -f "$req_file" ]]; then
    info "$(t "web_checking_deps")..."
    pip3 install -q -r "$req_file" || {
      error "$(t "web_pip_failed")"
      exit 1
    }
  fi

  local url="http://localhost:${port}"
  success "$(t "web_starting") $url"
  echo "  $(dim "$(t "web_stop_hint")")"
  echo ""

  # Open browser (macOS)
  if command -v open &>/dev/null; then
    (sleep 1.5 && open "$url") &
  fi

  # Start uvicorn
  cd "$cw_web_dir"
  exec python3 -m uvicorn main:app --port "$port" --host 127.0.0.1
}
```

**Step 2: Make executable**

```bash
chmod +x lib/web.sh
```

**Step 3: Commit**

```bash
git add lib/web.sh
git commit -m "feat(web): add lib/web.sh with cmd_web function"
```

---

## Task 9: Integrate cw web into bin/cw

**Files:**
- Modify: `bin/cw`

**Step 1: Add CW_WEB_DIR and source web.sh**

After line `CW_SKILLS_DIR=...` add:
```bash
CW_WEB_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../web" && pwd)"
```

After `source "$CW_LIB_DIR/lang.sh"` add:
```bash
source "$CW_LIB_DIR/web.sh"
```

**Step 2: Add web to usage()**

In the `usage()` function, after the `cw lang` line add:
```bash
  cw web [port]         $(t "usage_web")
```

**Step 3: Add web to main() case statement**

After `add-dir)` line add:
```bash
    web)          cmd_web "${@:2}" ;;
```

**Step 4: Verify**

```bash
# Run from repo root (not installed version)
./bin/cw help
```

Expected: `cw web [port]` appears in the usage output.

```bash
./bin/cw web
```

Expected: pip install runs (if needed), server starts on :8765, browser opens.

**Step 5: Commit**

```bash
git add bin/cw
git commit -m "feat(web): integrate cw web command into bin/cw"
```

---

## Task 10: Add i18n keys for web command

**Files:**
- Modify: `lib/i18n.sh`

**Step 1: Add keys to Japanese block**

In the `ja)` case block, after the last existing key before `*)`, add:

```bash
        # === Web UI ===
        "usage_web") echo "ローカル Web UI を起動" ;;
        "web_python_required") echo "Python 3 が必要です" ;;
        "web_checking_deps") echo "依存関係を確認しています" ;;
        "web_pip_failed") echo "pip install に失敗しました" ;;
        "web_starting") echo "Web UI を起動しました →" ;;
        "web_stop_hint") echo "Ctrl+C で停止" ;;
```

**Step 2: Add keys to English block**

In the `en)` case block (or `*)` fallback), add the same keys:

```bash
        # === Web UI ===
        "usage_web") echo "launch local Web UI server" ;;
        "web_python_required") echo "Python 3 is required" ;;
        "web_checking_deps") echo "Checking dependencies" ;;
        "web_pip_failed") echo "pip install failed" ;;
        "web_starting") echo "Web UI started →" ;;
        "web_stop_hint") echo "Press Ctrl+C to stop" ;;
```

**Step 3: Verify**

```bash
./bin/cw help
```

Expected: `cw web [port]` line shows translated description.

**Step 4: Commit**

```bash
git add lib/i18n.sh
git commit -m "feat(i18n): add web UI keys to i18n.sh"
```

---

## Task 11: Update install.sh for web/ directory

**Files:**
- Modify: `install.sh`

**Step 1: Add web/ copy step**

In `install.sh`, after the `cp "$CW_REPO_DIR/lib/"*.sh "$CW_HOME/lib/"` lines, add:

```bash
mkdir -p "$CW_HOME/web/templates/partials"
cp -r "$CW_REPO_DIR/web/"* "$CW_HOME/web/"
```

**Step 2: Add CW_WEB_DIR path rewrite in bin/cw sed command**

Locate the `sed` command that rewrites `CW_LIB_DIR` and `CW_SKILLS_DIR`, and add a `-e` for `CW_WEB_DIR`:

```bash
sed -e "s|CW_LIB_DIR=.*|CW_LIB_DIR=\"$CW_HOME/lib\"|" \
    -e "s|CW_SKILLS_DIR=.*|CW_SKILLS_DIR=\"$CW_HOME/skills\"|" \
    -e "s|CW_WEB_DIR=.*|CW_WEB_DIR=\"$CW_HOME/web\"|" \
  "$CW_REPO_DIR/bin/cw" > "/tmp/cw_install_tmp"
```

**Step 3: Verify end-to-end**

Run the full install:
```bash
bash install.sh
```

Then in a new shell:
```bash
cw web
```

Expected: Browser opens to `http://localhost:8765/` with the dashboard.

**Step 4: Commit**

```bash
git add install.sh
git commit -m "feat(install): copy web/ directory and rewrite CW_WEB_DIR on install"
```

---

## Task 12: Update CHANGELOG

**Files:**
- Modify: `docs/CHANGELOG.md`
- Modify: `docs/CHANGELOG_ja.md`

**Step 1: Add entry to `docs/CHANGELOG.md`** under `[Unreleased]`:

```markdown
### Added

- **`cw web` command** - Launch a local FastAPI + HTMX web UI for browser-based workspace management
  - Dashboard showing all workspaces with last-used dates and directory counts
  - Workspace detail page with directory list (HTMX partial updates)
  - Create new workspaces via web form
  - Launch workspace in Terminal, Forget (registry only), or Delete (with directory removal)
```

**Step 2: Add entry to `docs/CHANGELOG_ja.md`** under `[Unreleased]`:

```markdown
### 追加

- **`cw web` コマンド** - ブラウザベースでワークスペース管理ができるローカル Web UI（FastAPI + HTMX）を起動
  - 全ワークスペースを最終使用日・ディレクトリ数付きで表示するダッシュボード
  - ディレクトリ一覧（HTMX パーシャル更新）付きのワークスペース詳細ページ
  - Web フォームからの新規ワークスペース作成
  - Terminal 起動・Forget（レジストリのみ）・Delete（ディレクトリ含む）アクション
```

**Step 3: Commit**

```bash
git add docs/CHANGELOG.md docs/CHANGELOG_ja.md
git commit -m "docs: update CHANGELOG for cw web feature"
```

---

## Final Verification

```bash
# Ensure all existing commands still work
cw list
cw info
cw help

# Test web command
cw web
# → Browser opens to http://localhost:8765/
# → Dashboard shows workspaces
# → Click workspace → detail page
# → Add dir → HTMX updates list in place
# → New workspace form → creates and redirects
# → Launch → new Terminal opens with cw launch
# → Forget → workspace removed from list
```
